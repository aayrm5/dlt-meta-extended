{
    "expect_or_drop": {
        "valid_json": "validJson.is_error = false"
    },
    "expect_or_quarantine": {
        "quarantine_rule": "validJson.is_error = true and validJson.error_details IS NOT NULL"
    }
}